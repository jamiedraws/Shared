export interface IVimeoManagerPosterImageSpecification {
    /**
     * Represents the width in terms of pixels, but expressed as a unitless value
     */
    width?: string;
    /**
     * Represents the height in terms of pixels, but expressed as a unitless value
     */
    height?: string;
    /**
     * Represents the measure of quality in terms of percentage, but expressed as a unitless value
     */
    quality?: string;
}
